{
  "name": "final",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.6.1",
  "scripts": {
    "dev": "turbo run dev --filter=main-platform",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "latest"
  }
}
